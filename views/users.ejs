<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>使用者結果</title>
    </head>
    <body>
        <div class="container">
            <h2>使用者列表</h2>
            <table class="table table-bordered">
              <tr>
                <th>client_id</th>
                <th>game_id</th>
                <th>fish_count</th>
                <th>ship_count</th>
              </tr>
              <!-- 把"users"這個json塞到forEach，用user為單位一個一個取出-->
              <% users.forEach(function(user) { %>
                  <tr>
                    <td><%= user.client_id %></td>
                    <td><%= user.game_id %></td>
                    <td><%= user.fish_count %></td>
                    <td><%= user.ship_count %></td>
                  </tr>
              <% }); %>
            </table>
          </div>
    </body>
</html>